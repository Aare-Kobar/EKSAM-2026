services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      - DOMAIN=https://paroolihaldur.sinunimi.local
      - SIGNUPS_ALLOWED=true
      - ADMIN_TOKEN=Muuda_See_Turvaliseks_Tokeniks_123!
    volumes:
      - ./data:/data
    ports:
      - '8080:80'
